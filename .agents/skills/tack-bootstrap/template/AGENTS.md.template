# Agents

<!-- DEPRECATED as a Tack config surface. **Tack** uses repo-root **`TACK.md`** for orchestration, commands, and worktree policy — see `project/TACK.md.template`. Bootstrap no longer maintains this file. -->

This file is optional. If your tool requires `AGENTS.md`, keep a minimal pointer: load **`TACK.md`** at the repository root for Tack (SDD entry points, `tack-run` / `tack-agent`, `@project/prompts/auto-orchestrator.md`).

## Project layout

See **`TACK.md`** at repo root (`## Project layout`).

## Tack routing

See **`TACK.md`** (`## SDD entry points` and `## Auto-orchestration routing`).
