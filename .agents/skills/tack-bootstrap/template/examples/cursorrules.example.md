# Example: `.cursorrules` stub for Cursor (OrderFlow Checkout — fictitious)

Tack keys (`<TEST_COMMAND>`, `tack.worktree.*`, invariants, …) belong in **`TACK.md`** at the repo root — see **`TACK.md.example.md`** for a filled-out **`TACK.md`**.

This file is **optional** and Cursor-specific (editor hints only):

```markdown
# Cursor — repo rules (optional)

Project Tack configuration lives in **TACK.md** at the repository root. Do not duplicate quality commands here.

- Prefer imports from `src/lib/` over deep relative paths in new UI code.
```
