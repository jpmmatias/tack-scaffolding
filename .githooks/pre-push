#!/usr/bin/env bash
set -euo pipefail

GIT_DIR="$(git rev-parse --git-dir 2>/dev/null)" || exit 0
if [[ ! -w "$GIT_DIR" ]]; then
  exit 0
fi

cd "$(git rev-parse --show-toplevel)"

npm run --silent check-sync
npm run --silent validate-skill
npm run --silent check-routing
